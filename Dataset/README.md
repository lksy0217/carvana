This is where dataset and preprocessed data(npz file) are stored.

For dataset, you need directories(test, test_hq, train, train_hq, train_masks)
Each directories must contain images(jpg or gif) files directly.
